const config = {
    apiGateway: {
        REGION: "us-east-2",
        URL: "<URL>"
    },
    cognito: {
        REGION: "us-east-2",
        USER_POOL_ID: "<UserPoolID>",
        APP_CLIENT_ID: "<ClientID>"
    }
}

export default config;